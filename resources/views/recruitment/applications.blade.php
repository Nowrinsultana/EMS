<x-layout><div class="max-w-6xl mx-auto mt-10 px-4"><div class="bg-white rounded-lg shadow p-6"><h1 class="text-2xl font-bold">Candidates — {{ $vacancy->title }}</h1><p class="text-sm text-gray-500 mt-1 mb-6">Review applications, set progress, schedule interviews, and record selections.</p>@if(session('status'))<p class="mb-4 text-green-600">{{ session('status') }}</p>@endif<div class="space-y-5">@forelse($applications as $application)<div class="border rounded p-5"><div class="flex flex-wrap justify-between gap-3"><div><h2 class="font-semibold text-lg">{{ $application->name }}</h2><p class="text-sm text-gray-600">{{ $application->email }} · {{ $application->phone_number }}</p>@if($application->resume_url)<a class="text-sm text-indigo-600" target="_blank" href="{{ $application->resume_url }}">View resume</a>@endif</div><form method="POST" action="{{ route('recruitment.applications.update',['dptid'=>$dptid,'application'=>$application]) }}" class="flex gap-2">@csrf @method('PUT')<select name="status" class="border rounded p-2 text-sm">@foreach(['new','reviewing','interview','selected','rejected'] as $status)<option value="{{ $status }}" @selected($application->status===$status)>{{ ucfirst($status) }}</option>@endforeach</select><button class="border rounded px-3 text-sm">Update</button></form></div>@if($application->cover_letter)<p class="mt-3 text-sm text-gray-700 whitespace-pre-line">{{ $application->cover_letter }}</p>@endif<div class="mt-4 bg-gray-50 p-4 rounded"><h3 class="font-medium mb-2">Schedule interview</h3><form method="POST" action="{{ route('recruitment.interviews.store',['dptid'=>$dptid,'application'=>$application]) }}" class="grid md:grid-cols-4 gap-2">@csrf<input type="datetime-local" name="scheduled_at" class="border rounded p-2 text-sm" required><input name="interview_type" value="In-person" class="border rounded p-2 text-sm" required><input name="location" placeholder="Location / meeting link" class="border rounded p-2 text-sm"><button class="bg-indigo-600 text-white rounded px-3 py-2 text-sm">Schedule</button><textarea name="notes" placeholder="Notes (optional)" class="md:col-span-4 border rounded p-2 text-sm"></textarea></form>@foreach($application->interviews as $interview)<p class="mt-2 text-sm text-gray-600">Interview: {{ $interview->scheduled_at->format('d M Y, h:i A') }} · {{ $interview->interview_type }} · {{ $interview->location }}</p>@endforeach</div></div>@empty<p class="text-gray-500">No applications for this vacancy yet.</p>@endforelse</div></div></div></x-layout>
